rumba.mp3 — powers the global mini-player (bottom-left, every page).

Current file: 128kbps CBR, 44.1kHz, 3:34, ~3.4MB. Sourced from the
official @SoundsOfV12 YouTube upload of RUMBA and re-encoded for web
delivery, because the track is not on SoundCloud and no master was to
hand.

If Harold has the distributed master (the WAV/MP3 sent to EL V12 RECORDS
for the Spotify/Apple release), swap it in — it will sound better than a
re-encode of a lossy source. Nothing else needs to change; the player
looks for exactly this path and hides itself if the file is missing.
